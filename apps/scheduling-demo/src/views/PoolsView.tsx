import { PlaceholderView } from "./PlaceholderView";

export function PoolsView() {
  return (
    <PlaceholderView
      title="Pools"
      description="Manage the pool of candidates available to fill open shifts."
    />
  );
}
